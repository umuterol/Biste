import { StyleSheet, Text, View } from "react-native";
import React from "react";

const StationScreen = (props) => {
  return (
    <View>
      <Text>StationScreen</Text>
    </View>
  );
};

export default StationScreen;

const styles = StyleSheet.create({});
